# Copy to credentials.local.env in the project root and fill in your values.
# credentials.local.env is not versioned (.gitignore).

AIRBNB_EMAIL=your-email@example.com
AIRBNB_PASSWORD='replace-with-your-password'
# Use single quotes around the password if it contains special characters ($, ^, etc.)
